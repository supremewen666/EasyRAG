# Generation Pipeline

Placeholder for future teaching content.

This page will explain:
- where a future generation pipeline would sit after the current `QueryResult` stage
- how planned interfaces such as `AnswerParam`, `AnswerResult`, and `ContextAssemblyConfig` could fit
- how context builders, prompt builders, and answer synthesizers might extend EasyRAG later
